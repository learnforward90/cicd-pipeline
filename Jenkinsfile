pipeline {
  agent any
  stages {
    stage('pre-build') {
      steps {
        echo 'this is pre-build'
      }
    }

    stage('builf') {
      steps {
        echo 'build'
      }
    }

    stage('deploy') {
      steps {
        echo 'this is deploy'
      }
    }

  }
}